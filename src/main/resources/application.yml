spring:
  mvc:
    throw-exception-if-no-handler-found: true
  resources:
    add-mappings: false

server:
  port: 8080
  servlet:
    contextPath: /api

info:
  app:
    name: ${name}
    version: ${version}

debugging: true
